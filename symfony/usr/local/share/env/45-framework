#!/bin/bash

if [ "${SYMFONY_MAJOR_VERSION}" -lt 3 ]; then
  SYMFONY_CONSOLE_DEFAULT=${WORK_DIRECTORY}/bin/console
else
  SYMFONY_CONSOLE_DEFAULT=${WORK_DIRECTORY}/app/console
fi

export SYMFONY_CONSOLE=${SYMFONY_CONSOLE:-${SYMFONY_CONSOLE_DEFAULT}}
